const birthday = '18.04.1982';

const age = someCode(birthday);

const BIRTHDAY = '18.04.1982'; // make birthday uppercase? We can because it is unchangable

const AGE = someCode(BIRTHDAY); // make age uppercase? we shouldn't do it, because age is indeed changable